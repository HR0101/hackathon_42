<!-- File: GitHubRule.md -->

# GitHub ルール

## 基本ルール

- `main` ブランチには直接 `push` しない
- 必ず作業用ブランチを作って作業する
- 作業が終わったら Pull Request を作成する
- レビュー後に `main` へ `merge` する
- 作業前には最新の `main` を取り込む

---

## 作業の流れ

### 1. 最新のコードを取り込む

```bash
git checkout main
git pull origin main

GitHubチーム開発スタートガイド初めてGitHubでの共同開発に参加するメンバー向けの，Git/GitHubの使い方まとめです．作業中に迷ったときは，このドキュメントを上から順番に確認しながら進めていきましょう．1．Gitの全体像と基本用語Gitでは，ファイルが4つの場所を移動します．このイメージを頭に入れておくと，コマンドの意味がぐっと理解しやすくなります．[自分のPC（ローカル環境）]                                     [GitHub（リモート環境）]
 ワークスペース   --->   ステージングエリア   --->   ローカルリポジトリ   --->   リモートリポジトリ
 (作業フォルダ)   [add]   (買い物かご)      [commit] (変更履歴の保存所)  [push]   (みんなの共有フォルダ)
ワークスペース（Working Directory）: 実際にプログラムを書き換えている，自分のPC上の作業フォルダです．ステージングエリア（Staging Area）: コミット（保存）するファイルを一時的に並める「買い物かご」のような場所です．ローカルリポジトリ（Local Repository）: 自分のPC内にある，自分のためだけの履歴保存所です．リモートリポジトリ（Remote Repository）: GitHub上にある，チーム全員で共有する共有フォルダです．知っておきたい重要キーワードブランチ（Branch）: 本番のコード（main）を汚さないために，歴史を枝分かれさせて別部屋で作業する機能です．プルリクエスト（Pull Request / PR）: 「自分のブランチで作った機能を main に合流させていいですか？」とチームメンバーにレビューとマージを依頼するGitHub上の機能です．2．実践！開発の基本ワークフロー実際の開発では，基本的にこのステップ順にコマンドを実行していきます．STEP 1．プロジェクトを自分のPCに持ってくる（初回のみ）GitHub上のリポジトリを，自分のPC（ローカル環境）に丸ごとコピーします．git clone <リポジトリのURL>
STEP 2．最新の状態にしてから作業用ブランチを作る他のメンバーの変更を取りこぼさないよう，必ず最新の main ブランチから自分の作業部屋（ブランチ）を作ります．# 1．まずmainブランチに移動する
git checkout main

# 2．リモートリポジトリから最新のコードをダウンロードして反映する
git pull origin main

# 3．新しく自分の作業用ブランチを作って，そこに移動する
git checkout -b <新しいブランチ名>
💡 ブランチ名の付け方のコツ:feature/add-login-button や fix/bug-sidebar のように，接頭辞/作業内容 にするとひと目で目的がわかります．STEP 3．コードを編集して，変更を記録する自分のブランチでファイルを編集したら，その変更をローカルリポジトリに保存します．# 1．変更したファイルを「買い物かご（ステージング）」に入れる
git add .

# 2．買い物かごの中身をローカルリポジトリに記録（コミット）する
git commit -m "どのような変更をしたか分かりやすいメッセージ"
STEP 4．変更をGitHubにアップロードする自分のPCに保存した履歴を，GitHub（リモートリポジトリ）へ送信します．git push origin <自分のブランチ名>
STEP 5．GitHub上でプルリクエスト（PR）を作成してマージするGitHubのリポジトリページにアクセスすると，「Compare & pull request」 というボタンが表示されるのでクリックします．どのような変更を行ったかを説明文に書き，チームメンバーにレビューを依頼します．メンバーの確認と承認（Approve）をもらったら，「Merge pull request」 を押して main ブランチに合流させます．STEP 6．終わったブランチを片付けるマージが完了したら，不要になった手元の古いブランチを削除して整理します．# 1．mainブランチに戻る
git checkout main

# 2．役割が終わったローカルのブランチを削除する
git branch -d <削除するブランチ名>
3．チーム開発での超重要ルール初心者が特につまずきやすいポイントです．トラブルを防ぐために，チームの共通認識として共有しておきましょう．⚠️ 絶対に main ブランチで直接作業しない！main ブランチを直接編集してpushしてしまうと，他の人のコードと衝突してプロジェクトが壊れる原因になります．必ず新しくブランチを切ってから作業を始めましょう．🔄 新しい作業を始める前には必ず git pull をする！新しい作業を始める前や，新しくブランチを作る前には，必ず git checkout main してから git pull origin main を実行し，常に最新のコードをベースにしてください．✍️ コミットメッセージは具体的に書く！あとから履歴を見返したときに，誰が何をしたかわかるように記述します．❌ 良くない例：直した いろいろ修正 update⭕ 良い例：feat: ログイン画面のボタンデザインを変更 fix: サイドバーが表示されないバグを修正🤝 コンフリクト（衝突）したら触らずに相談する！git pull やマージをした際に，同じファイルの同じ行を複数人が同時に編集していると「Conflict（衝突）」が発生します．画面に変なエラーが出たら，無理に自力で解決しようとせず，すぐにDiscordなどでチームメンバーに画面を共有して一緒に解決しましょう． それが一番安全で確実な方法です．