# GitHubとGitコマンドガイド

## はじめに

このガイドは，チーム開発が初めての人を対象に，GitHub と Git の基本から実践的な使い方までをまとめたものです．
一人で開発しているときには気づかない「共同作業ならではの注意点」も詳しく解説しています．

---

## 1. そもそも Git と GitHub とは？

### Git とは
バージョン管理システムのひとつ．ファイルの変更履歴を記録し，「いつ」「誰が」「何を」変更したのかを追跡できます．過去のバージョンに戻ったり，複数人の変更を統合したりすることが可能です．

### GitHub とは
Git の仕組みを利用したWeb上のホスティングサービス．リポジトリをインターネット上に置き，世界中の人と共同開発できます．

> 💡 **イメージ**: Git は「履歴管理の仕組み」，GitHub は「みんなで集まる作業場所」．

---

## 2. 基本的な用語集

| 用語 | 説明 |
|------|------|
| **リポジトリ (repository)** | プロジェクトのファイルや変更履歴を保存する場所．． |
| **リモートリポジトリ** | GitHub 上にあるリポジトリ．チームで共有する「原本」． |
| **ローカルリポジトリ** | 自分のPC上にあるリポジトリ．ここで作業する． |
| **ブランチ (branch)** | 独立した作業ライン．本流（`main`）に影響を与えずに開発できる． |
| **クローン (clone)** | リモートリポジトリのコピーをローカルに作成すること． |
| **プル (pull)** | リモートから最新の変更を取得してローカルに反映すること． |
| **フェッチ (fetch)** | リモートの変更情報を取得するだけ（マージはしない）． |
| **アッド (add)** | 変更をコミット対象として「ステージング」すること． |
| **コミット (commit)** | ステージングした変更をローカルリポジトリに記録すること． |
| **プッシュ (push)** | ローカルの変更をリモートリポジトリに送信すること． |
| **マージ (merge)** | あるブランチの変更を別のブランチに統合すること． |
| **プルリクエスト (PR)** | 変更内容のレビュー・マージを依頼する仕組み． |
| **コンフリクト (conflict)** | 同じ箇所を複数人が編集したときに発生する競合． |
| **HEAD** | 現在チェックアウトしているブランチの最新コミットを指すポインタ． |
| **origin** | デフォルトのリモートリポジトリの名前（別名でも可）． |

---

## 3. 開発の全体的な流れ（フロー）

```
1. git clone        ← リモートからローカルにコピー（初回のみ）
2. git checkout -b  ← 作業用ブランチを作成
3. ファイルを編集
4. git add          ← 変更をステージング
5. git commit       ← ローカルに記録
6. git push         ← リモートに送信
7. Pull Request 作成 ← レビュー依頼
8. レビュー・修正
9. マージ            ← main に統合
10. ブランチ削除
```
### 3.1 開発の全体的な流れ（再開時）

```
1. git checkout main
2. git pull origin main
3. git checkout 自分のブランチ
4．git merge main


この流れを **1機能ごとに繰り返す**のが基本です．
```


## 4. 初期セットアップ（最初の1回だけ）

### Git のインストール確認
```bash
git --version
```

### ユーザー情報の設定
コミットには誰が変更したかの情報が記録されるため，必ず設定します．
```bash
git config --global user.name "あなたの名前"
git config --global user.email "your-email@example.com"
```

### 設定の確認
```bash
git config --list
```

---

## 5. 基本的な Git コマンド

###  リポジトリのクローン
GitHub上のリポジトリをローカルにコピーします．**初回のみ**実行．
```bash
git clone <リポジトリのURL>
```

実行後，リポジトリ名のフォルダが作成されるので，そこに移動します．
```bash
cd <リポジトリ名>
```

---

###  現在の状態を確認
```bash
git status        # 変更されたファイルやステージング状態を確認
git branch        # ブランチ一覧と現在地を確認
git log --oneline # コミット履歴を簡潔に表示
```

> 💡 **困ったらまず `git status`**．今どんな状態かを教えてくれます．

---

###  ブランチの作成と切り替え
**作業前に必ずブランチを作りましょう**．`main` で直接作業するのは厳禁です．

```bash
git checkout -b <新しいブランチ名>
```

#### ブランチ名の命名規則（推奨）
| 接頭辞 | 用途 | 例 |
|--------|------|----|
| `feature/` | 新機能の追加 | `feature/login-form` |
| `fix/` | バグ修正 | `fix/header-bug` |
| `docs/` | ドキュメント修正 | `docs/readme-update` |
| `refactor/` | リファクタリング | `refactor/api-client` |

---

###  変更のステージング
編集したファイルを，コミット対象として登録します．
```bash
git add <ファイル名>     # 特定のファイル
git add .                # 全ての変更
git add -p               # 変更箇所を1つずつ確認しながら追加（おすすめ）
```

---

###  変更のコミット
ステージングした変更をローカルリポジトリに保存します．
```bash
git commit -m "コミットメッセージ"
```

#### 良いコミットメッセージの書き方
**Prefix（接頭辞）を付ける**と分かりやすくなります．
```
feat: ログイン機能を追加
fix: ヘッダーの表示崩れを修正
docs: READMEに使い方を追記
style: インデントを修正
refactor: ユーザー認証処理を整理
test: ログイン機能のテストを追加
chore: 依存パッケージを更新
```

**悪い例**: `修正`, `更新`, `aaa`
**良い例**: `fix: ログインボタンが押せないバグを修正`

---

###  リモートリポジトリへのプッシュ
ローカルの変更を GitHub に送信します．
```bash
git push origin <ブランチ名>
```

初回プッシュ時は以下でも可（次回以降は `git push` だけでOK）:
```bash
git push -u origin <ブランチ名>
```

---

###  リモートからの取得と反映
他のメンバーの変更を取り込みます．

> ⚠️ **重要**: `git pull` する前には，**必ず `main` ブランチに戻ってから**行いましょう．

```bash
git checkout main
git pull origin main
```

その後，作業ブランチに戻って `main` の変更を取り込みます．
```bash
git checkout <作業ブランチ>
git merge main
```

---

###  ブランチのマージ
```bash
git checkout <マージ先のブランチ名>
git merge <マージ元のブランチ名>
```

> 💡 実際のチーム開発では，コマンドでマージするより **GitHub上のプルリクエスト経由でマージ**するのが一般的です．

---

###  ブランチの削除
```bash
git branch -d <削除するブランチ名>     # 安全な削除（マージ済みのみ）
git branch -D <削除するブランチ名>     # 強制削除
```

リモートのブランチを削除する場合:
```bash
git push origin --delete <ブランチ名>
```

---

## 6. プルリクエスト（PR）の作成手順

1. ブランチを GitHub にプッシュする．
2. GitHub のリポジトリページにアクセス．
3. **「Pull requests」タブ** → **「New pull request」** をクリック．
4. **base**（マージ先：通常は `main`）と **compare**（マージ元：作業ブランチ）を選択．
5. タイトルと説明を記入．
   - **何を変更したか**
   - **なぜ変更したか**
   - **どうテストしたか**
   - 関連 Issue 番号があれば記載（例: `Closes #12`）
6. レビュアーを指定して「Create pull request」をクリック．

### PR説明のテンプレート例
```
## 概要
ログイン機能を実装しました。

## 変更内容
- ログインフォームの追加
- 認証APIとの連携
- エラーメッセージの表示

## 動作確認
- [x] 正しいID/PWでログインできる
- [x] 誤ったPWでエラーが出る

## 関連Issue
Closes #15
```

---

## 7. コンフリクト（競合）の解決方法

同じファイルの同じ箇所を複数人が編集すると発生します．

### コンフリクトが起きるとファイルに以下のような表示が入ります:
```
<<<<<<< HEAD
自分の変更内容
=======
他の人の変更内容
>>>>>>> main
```

### 解決手順
1. ファイルを開いて，**どちらの変更を残すか（または両方を統合するか）** を決める．
2. `<<<<<<<`, `=======`, `>>>>>>>` の記号を全て削除する．
3. 再度コミットする．
```bash
git add <解決したファイル>
git commit -m "fix: コンフリクトを解決"
git push origin <ブランチ名>
```

> 💡 不安な場合は**必ずチームメンバーに相談**してから解決してください．

---

## 8. やってはいけないこと（初心者がやりがちなミス）

|  NG行動 | 理由 |
|----------|------|
| `main` ブランチで直接作業する | 他の人に影響する．必ずブランチを切る． |
| `git push --force` を気軽に使う | 他人のコミット履歴を破壊する可能性がある． |
| 巨大な変更を1つのコミットにする | レビューしづらく，原因特定も困難になる． |
| パスワードやAPIキーをコミットする | 公開リポジトリだと情報漏洩．`.gitignore` を活用． |
| コミットメッセージを「修正」だけにする | 後から見て何の修正か分からない． |
| pull せずに作業を始める | コンフリクトの原因になる． |

---

## 9. `.gitignore` ファイルについて

Git で管理したくないファイル（ログ，環境変数，ビルド成果物など）を指定します．

### `.gitignore` の例
```
# 環境変数
.env

# ログファイル
*.log

# OS固有のファイル
.DS_Store
Thumbs.db

# 依存パッケージ
node_modules/
__pycache__/

# IDEの設定
.vscode/
.idea/
```

---

## 10. よく使うコマンドのチートシート

```bash
# 状況確認
git status                          # 現在の状態を確認
git log --oneline --graph --all     # コミット履歴を可視化

# やり直し系
git restore <ファイル名>             # 編集を取り消す
git restore --staged <ファイル名>    # ステージングを取り消す
git commit --amend -m "新メッセージ" # 直前のコミットメッセージを修正

# ブランチ操作
git branch                          # ブランチ一覧
git checkout <ブランチ名>            # ブランチ切り替え
git switch <ブランチ名>              # 切り替え（新しい書き方）

# 差分確認
git diff                            # 編集中の差分
git diff --staged                   # ステージング済みの差分
```

---

## 11. 困ったときの対処法

### 「間違えてコミットしてしまった」
```bash
git reset --soft HEAD~1   # コミットだけ取り消し（変更は残る）
```

### 「間違えたブランチで作業してしまった」
```bash
git stash                 # 変更を一時保存
git checkout <正しいブランチ>
git stash pop             # 保存した変更を復元
```

### 「`git pull` でエラーが出る」
ローカルに未コミットの変更があると pull できません．
```bash
git stash         # 一時退避
git pull origin main
git stash pop     # 戻す
```

---

## 12. チーム開発で大切な心構え

1. **こまめにコミット・プッシュする** — 作業の見える化につながる．
2. **`git pull` を習慣にする** — コンフリクトを未然に防ぐ．
3. **わからないときはすぐ聞く** — 強制プッシュで取り返しがつかなくなる前に．
4. **コミットメッセージは未来の自分とチームへの手紙** — 丁寧に書く．
5. **レビューは批判ではなく学びの場** — 指摘されたら感謝して直す．
```